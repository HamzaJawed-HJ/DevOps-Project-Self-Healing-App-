version: "3.9"

services:
  api:
    build: ./services/api
    container_name: api-container
    ports:
      - "3000:3000"
    restart: always
    labels:
      autoheal: "true"   # 👈 remediator will use this later
